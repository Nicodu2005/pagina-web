import React, { useContext } from 'react';
import { cartContext } from "../interfaces/ContexrCart";
import type { cartContextType } from '../interfaces/cart';

const Carrito = (carrito:cartContextType) => {
  
  
    return (
    <div>
      
    </div>
  )
}

export default Carrito
